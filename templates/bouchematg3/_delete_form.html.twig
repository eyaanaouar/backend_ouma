<form method="post" action="{{ path('app_bouchematg3_delete', {'id': bouchematg3.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bouchematg3.id) }}">
    <button class="btn">Delete</button>
</form>
