<form method="post" action="{{ path('app_bouchematg4_delete', {'id': bouchematg4.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bouchematg4.id) }}">
    <button class="btn">Delete</button>
</form>
