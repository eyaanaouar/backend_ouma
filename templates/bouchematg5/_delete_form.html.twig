<form method="post" action="{{ path('app_bouchematg5_delete', {'id': bouchematg5.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bouchematg5.id) }}">
    <button class="btn">Delete</button>
</form>
