<form method="post" action="{{ path('app_ferianatg1_delete', {'id': ferianatg1.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ferianatg1.id) }}">
    <button class="btn">Delete</button>
</form>
