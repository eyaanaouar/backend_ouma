<form method="post" action="{{ path('app_ferianatg2_delete', {'id': ferianatg2.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ferianatg2.id) }}">
    <button class="btn">Delete</button>
</form>
