<form method="post" action="{{ path('app_goulettetg1_delete', {'id': goulettetg1.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ goulettetg1.id) }}">
    <button class="btn">Delete</button>
</form>
