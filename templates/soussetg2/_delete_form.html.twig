<form method="post" action="{{ path('app_soussetg2_delete', {'id': soussetg2.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ soussetg2.id) }}">
    <button class="btn">Delete</button>
</form>
