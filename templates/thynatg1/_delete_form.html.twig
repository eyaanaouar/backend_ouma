<form method="post" action="{{ path('app_thynatg1_delete', {'id': thynatg1.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ thynatg1.id) }}">
    <button class="btn">Delete</button>
</form>
