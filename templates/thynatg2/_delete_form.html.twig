<form method="post" action="{{ path('app_thynatg2_delete', {'id': thynatg2.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ thynatg2.id) }}">
    <button class="btn">Delete</button>
</form>
